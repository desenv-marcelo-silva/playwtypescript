import { User } from './models/User';

//const user = new User({ id: 1 });

// user.set({ name: 'Marcelo Silva', age: 56 });

// user.save();

const user = new User({ name: 'Aline', age: 34 });

user.save();
